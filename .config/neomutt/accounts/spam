# --- Spam Account Configuration ---

# Sender identification
set from = "spam@keilholz.biz"

# Path for sent messages
set record = "+spam/Gesendet"

# SMTP settings
set sendmail = "/usr/bin/msmtp -a allinkl"

# No PGP/Signature for this address
unset pgp_autosign
unset pgp_autoencrypt
unset signature

